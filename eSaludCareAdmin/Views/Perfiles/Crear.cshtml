@model CapaEntidad.PerfilUsuarioDTO
@{
    ViewBag.Title = "Agregar Usuario";
    var roles = new List<SelectListItem>
{
        new SelectListItem { Text = "admin", Value = "admin" },
        new SelectListItem { Text = "medico", Value = "medico" },
        new SelectListItem { Text = "paciente", Value = "paciente" }
    };
}

<h2>Agregar Nuevo Usuario</h2>

@using (Html.BeginForm("Crear", "Perfiles", FormMethod.Post))
{
    <div class="form-group">
        <label>Nombre</label>
        @Html.TextBoxFor(m => m.Nombre, new { @class = "form-control" })
    </div>

    <div class="form-group">
        <label>Correo</label>
        @Html.TextBoxFor(m => m.Correo, new { @class = "form-control" })
    </div>

    <div class="form-group">
        <label>Teléfono</label>
        @Html.TextBoxFor(m => m.Telefono, new { @class = "form-control" })
    </div>

    <div class="form-group">
        <label>Rol</label>
        @Html.DropDownListFor(m => m.Rol, roles, "Seleccione un rol", new { @class = "form-control" })
    </div>

    <div class="form-group">
        <label>Contraseña</label>
        @Html.PasswordFor(m => m.Contrasena, new { @class = "form-control", required = "required" })
    </div>

    <button type="submit" class="btn btn-success">Crear Usuario</button>
    <a href="@Url.Action("Index", "Perfiles")" class="btn btn-secondary">Cancelar</a>
}
